import Event from "../../../models/Event";


const eventController = async (action) => {
    switch (action.type) {
        default:
            throw new Error("Your query not defined")
    }
}
export default eventController